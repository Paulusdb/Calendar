<!DOCTYPE html>
<html>
    <head>
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        {# Run `composer require symfony/webpack-encore-bundle`
           and uncomment the following Encore helpers to start using Symfony UX #}
        {% block stylesheets %}
            {#{{ encore_entry_link_tags('app') }}#}
        {% endblock %}

        {% block javascripts %}
            {#{{ encore_entry_script_tags('app') }}#}
        {% endblock %}
    </head>
    <body>
    <div class="container">
        <div class="row">
            <div class="col">
                <a class="navbar-brand" href="{{ path('app_calendar') }}">
                    <h1 class="mt-5">{% block pagetitle %}SkreprCalendar{% endblock %}</h1>
                </a>
                <div class="float-end">
                    <a class="btn btn-outline-primary btn-sm" href="{{ path('appointment_new') }}" role="button">Create new appointment</a>
                    <a class="btn btn-outline-primary btn-sm" href="{{ path('appointment_index') }}" role="button">All appointments (list)</a>
                </div>
            </div>
        </div>
        <div class="row">
            <div class="col">
                <h3>{% block page_subtitle %}{% endblock %}</h3>
                {% block body %}{% endblock %}
            </div>
        </div>
        <div class="row">
            <div class="col">
                <p class="mt-5">Made with <span class="text-danger"><3</span> by Paulusdb</p>
            </div>
        </div>
    </div>
    </body>
</html>
